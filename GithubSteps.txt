# 1. Navigate to project
cd C:\Users\HP\Desktop\Bank_Trust_RFM

# Removing any previous commits, not to be used everything, only used it because i want to show you from scratch.
rmdir /s /q .git

# 2. Create .gitignore file with Data/ and .env

# 3. Configure git
git config --global user.name "Olorioko Data"
git config --global user.email "Kolawoleola2001@gmail.com"

# 4. Initialize repository
git init

# 5. Add and commit
git add .
git commit -m "Initial commit: Bank Trust RFM Project"

# 6. Add remote
git remote add origin https://github.com/Olorioko-Data/BANK_TRUST_RFM_PROJECT.git

# 7. Rename branch and push
git branch -M main
git push -u origin main

#8. Check which file changes was made
git status

#9. add all changes
git add .

#10. Commit the changes
git commit -m "Add new feature: description of changes"

#11. Push to GitHub
git push origin main
